# Python deps for the remaining `scripts/generate_poster_collage.py` utility.
# All TMDB / image-download / blurhash Python code has been migrated to the
# Node implementation under `node/`.
Pillow
python-dotenv
